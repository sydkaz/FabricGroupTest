/*
package com.geekheights.app;

import com.geekheights.app.consts.AppConstants;

public interface ResidentBillingStrategy {
    Double getActPrice(Double priceFactor);

    static ResidentBillingStrategy applyPricing() {
        return (priceFactor) -> ratioA / (ratioA+ratioB) * AppConstants._2BHK.getText() * AppConstants.MAX_LITERS_PER_PERSON.getText() * AppConstants.NO_OF_DAYS.getText();
    }
}
*/
